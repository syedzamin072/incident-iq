route:
  receiver: "webhook-log"
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: "webhook-log"
    webhook_configs:
      - url: "http://host.docker.internal:5678/webhook/incident-alert"
        send_resolved: true