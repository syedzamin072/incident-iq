[project]
name = "agent-service"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "sentence-transformers",
    "faiss-cpu",
    "numpy",
    "langgraph",
    "fastapi",
    "uvicorn[standard]",
]